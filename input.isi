programa

declare a,b,c,d.
escreva("Programa Teste").
escreva ("Digite A").
leia (a).
escreva ("Digite B").
leia (b).

if (a<b)
{
 c := a + b.
 c := a + b.
}else
{
 c := a - b.
 c := a - b.
}

escreva ("C e igual a ").
escreva (c).

d := c * a + b.

do
{
 c := c - 1.
}
while(c > d).

while(d > 0)
{
 d := d - 1.
 d := d - 2.
}

escreva ("D e igual a ").
escreva (d).



fimprog.